import {ACTIONS} from "./types";

export default {
    [ACTIONS.LOAD_MESSAGES]({commit, state}, pageToken) {

    },

    [ACTIONS.LOAD_NEXT_PAGE]({commit, state}) {

    },

    [ACTIONS.LOAD_PREV_PAGE]({commit, state}) {

    },

    [ACTIONS.OPEN_MESSAGE]({commit, state}, index) {

    }
}
